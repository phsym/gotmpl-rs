[package]
name = "gotmpl-benches"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
gotmpl = { path = ".." }

[dev-dependencies]
criterion = { version = "0.8", default-features = false, features = ["cargo_bench_support"] }

[[bench]]
name = "template"
harness = false
