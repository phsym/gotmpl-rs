[package]
name = "gotmpl"
version = "0.1.0"
edition = "2024"
description = "A Rust reimplementation of Go's text/template library"

[features]
default = ["std"]
std = []
go-crosscheck = []

[dependencies]
thiserror = "2"
